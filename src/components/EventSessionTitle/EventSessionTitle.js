import React from 'react'

const EventSessionTitle = ({ children }) => (
  <h1 className="h1">
    <span className="square"></span>
    <strong>{children}</strong>
  </h1>
)

export default EventSessionTitle
